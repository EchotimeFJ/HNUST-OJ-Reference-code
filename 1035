#include<stdio.h>
int main()
{
    long long sum,n;
    while(~scanf("%lld",&n))
    {   sum=1;
        for(int i=1; i<=n; i++)
        {
            sum=sum*3;
        }
        printf("%lld\n",sum-1);
    }
    return 0;
}
